# ── Dependencies ─────────────────────────────────────────────
node_modules/

# ── Build output ─────────────────────────────────────────────
# NOTE: these are intentionally unanchored so they match the nested
# workspaces too (crates/nabu-ui, crates/nabu-core, src-tauri each
# have their own target/ and dist/).
target/
dist/
out/
generated/
*.rs.bk

# ── Stray screen-capture artifact ────────────────────────────
# macOS `screencapture` (used by crates/nabu-core native/screenshot.rs)
# historically wrote a PNG to a file literally named "-" in the cwd; that
# file must never be tracked. (The capture code now uses a temp file.)
crates/nabu-core/-

# ── Test / lint artifacts ────────────────────────────────────
test-results/
playwright-report/
*.tsbuildinfo

# ── Editor / OS ──────────────────────────────────────────────
.DS_Store
*.log
.idea/
.vscode/
.kiro/
.zcode/

# ── Mise local overrides ─────────────────────────────────────
.mise.local.toml
.mise/*.env
core

# ── Nabu runtime data (vault-local files created by the app) ─
.nabu/

# ── Qodana / JetBrains inspection results (regenerated each run) ─
*.xml
*.descriptions.xml
